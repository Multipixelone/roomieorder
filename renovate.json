{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": [
    "Dependency-update lanes are split so Renovate and the flake-update workflow don't both touch the lockfile:",
    "Renovate owns pyproject.toml deps and GitHub Action digests;",
    ".github/workflows/flake-update.yml owns flake.lock.",
    "The nix manager is disabled below so Renovate never opens a flake.lock PR."
  ],
  "extends": [
    "config:recommended",
    "helpers:pinGitHubActionDigests"
  ],
  "automerge": true,
  "automergeType": "pr",
  "automergeStrategy": "squash",
  "platformAutomerge": true,
  "semanticCommits": "enabled",
  "prConcurrentLimit": 5,
  "nix": {
    "enabled": false
  }
}
